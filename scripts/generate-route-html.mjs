/**
 * After Vite build, optionally write per-route HTML shells for marketing SEO.
 * Private personal app: skip — site is login-only and noindexed; do not emit
 * static marketing/blog HTML that would bypass the SPA redirect to /login.
 */
console.log('SEO HTML shells skipped (private personal app — login-only, noindex)')
